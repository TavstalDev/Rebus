package io.github.tavstaldev.rebus.models;

/**
 * Represents the type of prize reward.
 */
public enum EPrizeType {
    /** The prize grants an item. */
    ITEM,
    /** The prize executes a command. */
    COMMAND
}
